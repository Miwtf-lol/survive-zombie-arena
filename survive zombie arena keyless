-- MIU HUB - Survive Zombie Arena [FIXED VERSION]

-- Đợi game load
repeat task.wait() until game:IsLoaded()

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local VirtualInputManager = game:GetService("VirtualInputManager")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Kiểm tra remote
local gunRemotes = ReplicatedStorage:FindFirstChild("GunRemotes")
if not gunRemotes then
    warn("[MIU HUB] GunRemotes not found! Searching...")
    -- Tìm trong tất cả children
    for _, v in ipairs(ReplicatedStorage:GetChildren()) do
        if v:IsA("Folder") or v:IsA("Configuration") then
            if v:FindFirstChild("GunHit") or v:FindFirstChild("GunFire") then
                gunRemotes = v
                break
            end
        end
    end
end

local gunHit = gunRemotes and (gunRemotes:FindFirstChild("GunHit") or gunRemotes:FindFirstChildWhichIsA("RemoteEvent"))
local gunFire = gunRemotes and (gunRemotes:FindFirstChild("GunFire") or gunRemotes:FindFirstChildWhichIsA("RemoteEvent"))

if not gunHit then
    warn("[MIU HUB] GunHit remote not found! Trying fallback...")
    -- Fallback: tìm bất kỳ remote nào liên quan đến súng
    for _, v in ipairs(ReplicatedStorage:GetDescendants()) do
        if v:IsA("RemoteEvent") and (v.Name:lower():find("gun") or v.Name:lower():find("hit") or v.Name:lower():find("shoot") or v.Name:lower():find("fire") or v.Name:lower():find("damage")) then
            gunHit = v
            break
        end
    end
end

if not gunFire then
    gunFire = gunHit -- fallback: dùng chung remote
end

print("[MIU HUB] GunHit:", gunHit and gunHit:GetFullName() or "NOT FOUND")
print("[MIU HUB] GunFire:", gunFire and gunFire:GetFullName() or "NOT FOUND")

-- ==========================================
-- THÔNG BÁO KHỞI ĐỘNG
-- ==========================================

local notificationGui = Instance.new("ScreenGui")
notificationGui.Name = "MIUNotification"
notificationGui.ResetOnSpawn = false
notificationGui.Parent = playerGui

local notifBg = Instance.new("Frame")
notifBg.Size = UDim2.new(0, 0, 0, 0)
notifBg.Position = UDim2.new(0.5, -200, 0.5, -40)
notifBg.BackgroundColor3 = Color3.fromRGB(255, 50, 150)
notifBg.BackgroundTransparency = 0.15
notifBg.BorderSizePixel = 0
notifBg.AnchorPoint = Vector2.new(0.5, 0.5)
notifBg.Parent = notificationGui

local notifCorner = Instance.new("UICorner")
notifCorner.CornerRadius = UDim.new(0, 15)
notifCorner.Parent = notifBg

local notifTitle = Instance.new("TextLabel")
notifTitle.Size = UDim2.new(1, 0, 0.6, 0)
notifTitle.Position = UDim2.new(0, 0, 0.05, 0)
notifTitle.BackgroundTransparency = 1
notifTitle.Text = "🌸 MIU HUB"
notifTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
notifTitle.Font = Enum.Font.GothamBold
notifTitle.TextSize = 30
notifTitle.TextScaled = true
notifTitle.Parent = notifBg

local notifSub = Instance.new("TextLabel")
notifSub.Size = UDim2.new(1, 0, 0.35, 0)
notifSub.Position = UDim2.new(0, 0, 0.6, 0)
notifSub.BackgroundTransparency = 1
notifSub.Text = "Survive Zombie Arena | Loading..."
notifSub.TextColor3 = Color3.fromRGB(255, 200, 230)
notifSub.Font = Enum.Font.Gotham
notifSub.TextSize = 14
notifSub.TextScaled = true
notifSub.Parent = notifBg

-- Animation
local expandTween = TweenService:Create(notifBg, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, 400, 0, 80),
    BackgroundTransparency = 0.15
})
expandTween:Play()

task.wait(2)

local fadeTween = TweenService:Create(notifBg, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
    BackgroundTransparency = 1
})
fadeTween:Play()
notifTitle.TextTransparency = 1
notifSub.TextTransparency = 1

task.wait(0.5)
notificationGui:Destroy()

-- ==========================================
-- CẤU HÌNH
-- ==========================================

local CONFIG = {
    KillRadius = 200,
    MaxRadius = 500,
    MinRadius = 10,
    FireRate = 0.05,
    InfiniteAmmo = true,
    MultiHitPerEnemy = 3,
    FlySpeed = 50,
}

-- BIẾN
local auraEnabled = false
local flyEnabled = false
local currentRadius = CONFIG.KillRadius
local totalKills = 0
local killLoop = nil
local flyConnection = nil
local bodyGyro = nil
local bodyVelocity = nil

-- ==========================================
-- HÀM KILL AURA (ĐƠN GIẢN HÓA)
-- ==========================================

local function getEnemiesInRange(radius)
    local enemies = {}
    local character = player.Character
    if not character then return enemies end
    
    local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
    if not humanoidRootPart then return enemies end
    
    local position = humanoidRootPart.Position
    
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("Model") then
            local humanoid = obj:FindFirstChild("Humanoid")
            if humanoid and humanoid.Health > 0 then
                if not Players:GetPlayerFromCharacter(obj) then
                    local root = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChild("Torso") or obj:FindFirstChild("UpperTorso")
                    if root then
                        local dist = (root.Position - position).Magnitude
                        if dist <= radius then
                            table.insert(enemies, obj)
                        end
                    end
                end
            end
        end
    end
    
    return enemies
end

local function fireGunHit(target)
    if not gunHit then return false end
    
    local root = target:FindFirstChild("HumanoidRootPart") or target:FindFirstChild("Torso") or target:FindFirstChild("UpperTorso")
    if not root then return false end
    
    local hitPos = root.Position + Vector3.new(
        math.random(-300, 300) / 100,
        math.random(-200, 200) / 100,
        math.random(-300, 300) / 100
    )
    local hitNormal = Vector3.new(0, 1, 0)
    
    local success = pcall(function()
        gunHit:FireServer(target, hitPos, hitNormal)
    end)
    
    return success
end

local function fireGunEffect()
    if not gunFire then return end
    local character = player.Character
    if not character then return end
    local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
    if not humanoidRootPart then return end
    
    pcall(function()
        gunFire:FireServer(humanoidRootPart.Position, humanoidRootPart.CFrame.LookVector)
    end)
end

local function handleAmmo()
    local character = player.Character
    if not character then return true end
    
    local tool = character:FindFirstChildOfClass("Tool")
    if not tool then return true end
    
    if CONFIG.InfiniteAmmo then
        -- Tìm biến đạn và set về max
        for _, child in ipairs(tool:GetChildren()) do
            if child:IsA("NumberValue") or child:IsA("IntValue") then
                local name = child.Name:lower()
                if name:find("ammo") or name:find("bullet") or name:find("clip") or name:find("magazine") then
                    local maxAmmo = tool:FindFirstChild("MaxAmmo") or tool:FindFirstChild("MaxBullets")
                    if maxAmmo then
                        child.Value = maxAmmo.Value
                    else
                        child.Value = 999
                    end
                    break
                end
            end
        end
    end
    
    return true
end

local function startKillAura()
    if auraEnabled then return end
    auraEnabled = true
    
    print("[MIU HUB] Kill Aura Started - Radius:", currentRadius)
    
    killLoop = RunService.Heartbeat:Connect(function()
        if not auraEnabled then return end
        
        local character = player.Character
        if not character then return end
        
        -- Kiểm tra có tool (súng) không
        local tool = character:FindFirstChildOfClass("Tool")
        if not tool then return end
        
        -- Xử lý đạn
        handleAmmo()
        
        -- Gửi hiệu ứng bắn
        fireGunEffect()
        
        -- Lấy quái trong bán kính và bắn
        local enemies = getEnemiesInRange(currentRadius)
        
        for _, enemy in ipairs(enemies) do
            for i = 1, CONFIG.MultiHitPerEnemy do
                fireGunHit(enemy)
                task.wait(0.01)
            end
        end
        
        -- Delay nhẹ
        if #enemies > 0 then
            task.wait(CONFIG.FireRate)
        end
    end)
end

local function stopKillAura()
    auraEnabled = false
    if killLoop then
        killLoop:Disconnect()
        killLoop = nil
    end
    print("[MIU HUB] Kill Aura Stopped")
end

-- ==========================================
-- HÀM FLY (ĐƠN GIẢN HÓA)
-- ==========================================

local function startFly()
    if flyEnabled then return end
    
    local character = player.Character
    if not character then return end
    
    local humanoid = character:FindFirstChild("Humanoid")
    local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
    
    if not humanoid or not humanoidRootPart then return end
    
    flyEnabled = true
    humanoid.PlatformStand = true
    humanoid.AutoRotate = false
    
    -- Tạo body mover
    bodyGyro = Instance.new("BodyGyro")
    bodyGyro.P = 9e4
    bodyGyro.D = 9e3
    bodyGyro.MaxTorque = Vector3.new(9e5, 9e5, 9e5)
    bodyGyro.CFrame = workspace.CurrentCamera.CFrame
    bodyGyro.Parent = humanoidRootPart
    
    bodyVelocity = Instance.new("BodyVelocity")
    bodyVelocity.Velocity = Vector3.new(0, 0, 0)
    bodyVelocity.MaxForce = Vector3.new(9e5, 9e5, 9e5)
    bodyVelocity.Parent = humanoidRootPart
    
    print("[MIU HUB] Fly Enabled - Speed:", CONFIG.FlySpeed)
    
    flyConnection = RunService.RenderStepped:Connect(function()
        if not flyEnabled or not character or not humanoidRootPart or not humanoid then
            stopFly()
            return
        end
        
        local moveDirection = Vector3.new(0, 0, 0)
        local camera = workspace.CurrentCamera
        
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            moveDirection = moveDirection + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            moveDirection = moveDirection - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            moveDirection = moveDirection - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            moveDirection = moveDirection + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            moveDirection = moveDirection + Vector3.new(0, 1, 0)
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then
            moveDirection = moveDirection + Vector3.new(0, -1, 0)
        end
        
        if moveDirection.Magnitude > 0 then
            moveDirection = moveDirection.Unit * CONFIG.FlySpeed
        end
        
        bodyVelocity.Velocity = moveDirection
        bodyGyro.CFrame = camera.CFrame
    end)
end

local function stopFly()
    flyEnabled = false
    
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    
    if bodyGyro then
        bodyGyro:Destroy()
        bodyGyro = nil
    end
    
    if bodyVelocity then
        bodyVelocity:Destroy()
        bodyVelocity = nil
    end
    
    local character = player.Character
    if character then
        local humanoid = character:FindFirstChild("Humanoid")
        if humanoid then
            humanoid.PlatformStand = false
            humanoid.AutoRotate = true
        end
    end
    
    print("[MIU HUB] Fly Disabled")
end

-- ==========================================
-- GUI CHÍNH - MÀU HỒNG
-- ==========================================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MIUHubGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = playerGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 280, 0, 320)
MainFrame.Position = UDim2.new(0.5, -140, 0.5, -160)
MainFrame.BackgroundColor3 = Color3.fromRGB(255, 80, 170)
MainFrame.BackgroundTransparency = 0.1
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

-- Title Bar
local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 40)
TitleBar.BackgroundColor3 = Color3.fromRGB(200, 40, 130)
TitleBar.BackgroundTransparency = 0.2
TitleBar.BorderSizePixel = 0
local TitleBarCorner = Instance.new("UICorner")
TitleBarCorner.CornerRadius = UDim.new(0, 12)
TitleBarCorner.Parent = TitleBar
TitleBar.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 1, 0)
Title.BackgroundTransparency = 1
Title.Text = "🌸 MIU HUB"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 18
Title.TextScaled = true
Title.Parent = TitleBar

-- Close Button
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 28, 0, 28)
CloseBtn.Position = UDim2.new(1, -34, 0, 6)
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 14
local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseBtn
CloseBtn.Parent = TitleBar

-- === KILL AURA ===
local function createLine(parent, yPos)
    local line = Instance.new("Frame")
    line.Size = UDim2.new(0.9, 0, 0, 1)
    line.Position = UDim2.new(0.05, 0, yPos, 0)
    line.BackgroundColor3 = Color3.fromRGB(255, 150, 200)
    line.BackgroundTransparency = 0.5
    line.BorderSizePixel = 0
    line.Parent = parent
end

-- Section Label
local auraLabel = Instance.new("TextLabel")
auraLabel.Size = UDim2.new(0.9, 0, 0, 22)
auraLabel.Position = UDim2.new(0.05, 0, 0.14, 0)
auraLabel.BackgroundTransparency = 1
auraLabel.Text = "🎯 KILL AURA"
auraLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
auraLabel.Font = Enum.Font.GothamBold
auraLabel.TextSize = 13
auraLabel.TextXAlignment = Enum.TextXAlignment.Left
auraLabel.Parent = MainFrame

createLine(MainFrame, 0.21)

-- Aura Status
local AuraStatus = Instance.new("TextLabel")
AuraStatus.Size = UDim2.new(0.9, 0, 0, 18)
AuraStatus.Position = UDim2.new(0.05, 0, 0.22, 0)
AuraStatus.BackgroundTransparency = 1
AuraStatus.Text = "Status: OFF | Range: " .. currentRadius
AuraStatus.TextColor3 = Color3.fromRGB(255, 180, 210)
AuraStatus.Font = Enum.Font.Gotham
AuraStatus.TextSize = 10
AuraStatus.TextXAlignment = Enum.TextXAlignment.Left
AuraStatus.Parent = MainFrame

-- Toggle Aura
local AuraBtn = Instance.new("TextButton")
AuraBtn.Size = UDim2.new(0.42, 0, 0, 28)
AuraBtn.Position = UDim2.new(0.05, 0, 0.28, 0)
AuraBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 160)
AuraBtn.Text = "▶ Aura ON"
AuraBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
AuraBtn.Font = Enum.Font.GothamBold
AuraBtn.TextSize = 11
local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 6)
btnCorner.Parent = AuraBtn
AuraBtn.Parent = MainFrame

-- Radius Input
local RadiusInput = Instance.new("TextBox")
RadiusInput.Size = UDim2.new(0.42, 0, 0, 28)
RadiusInput.Position = UDim2.new(0.53, 0, 0.28, 0)
RadiusInput.BackgroundColor3 = Color3.fromRGB(200, 50, 140)
RadiusInput.Text = tostring(currentRadius)
RadiusInput.TextColor3 = Color3.fromRGB(255, 255, 255)
RadiusInput.Font = Enum.Font.GothamBold
RadiusInput.TextSize = 12
RadiusInput.PlaceholderText = "Radius"
local inputCorner = Instance.new("UICorner")
inputCorner.CornerRadius = UDim.new(0, 6)
inputCorner.Parent = RadiusInput
RadiusInput.Parent = MainFrame

-- === FLY ===
local flyLabel = Instance.new("TextLabel")
flyLabel.Size = UDim2.new(0.9, 0, 0, 22)
flyLabel.Position = UDim2.new(0.05, 0, 0.40, 0)
flyLabel.BackgroundTransparency = 1
flyLabel.Text = "🕊️ FLY"
flyLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
flyLabel.Font = Enum.Font.GothamBold
flyLabel.TextSize = 13
flyLabel.TextXAlignment = Enum.TextXAlignment.Left
flyLabel.Parent = MainFrame

createLine(MainFrame, 0.47)

-- Fly Status
local FlyStatus = Instance.new("TextLabel")
FlyStatus.Size = UDim2.new(0.9, 0, 0, 18)
FlyStatus.Position = UDim2.new(0.05, 0, 0.48, 0)
FlyStatus.BackgroundTransparency = 1
FlyStatus.Text = "Status: OFF | Speed: " .. CONFIG.FlySpeed
FlyStatus.TextColor3 = Color3.fromRGB(255, 180, 210)
FlyStatus.Font = Enum.Font.Gotham
FlyStatus.TextSize = 10
FlyStatus.TextXAlignment = Enum.TextXAlignment.Left
FlyStatus.Parent = MainFrame

-- Toggle Fly
local FlyBtn = Instance.new("TextButton")
FlyBtn.Size = UDim2.new(0.42, 0, 0, 28)
FlyBtn.Position = UDim2.new(0.05, 0, 0.54, 0)
FlyBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 160)
FlyBtn.Text = "▶ Fly ON"
FlyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FlyBtn.Font = Enum.Font.GothamBold
FlyBtn.TextSize = 11
local flyBtnCorner = Instance.new("UICorner")
flyBtnCorner.CornerRadius = UDim.new(0, 6)
flyBtnCorner.Parent = FlyBtn
FlyBtn.Parent = MainFrame

-- Speed Input
local SpeedInput = Instance.new("TextBox")
SpeedInput.Size = UDim2.new(0.42, 0, 0, 28)
SpeedInput.Position = UDim2.new(0.53, 0, 0.54, 0)
SpeedInput.BackgroundColor3 = Color3.fromRGB(200, 50, 140)
SpeedInput.Text = tostring(CONFIG.FlySpeed)
SpeedInput.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedInput.Font = Enum.Font.GothamBold
SpeedInput.TextSize = 12
SpeedInput.PlaceholderText = "Speed"
local speedCorner = Instance.new("UICorner")
speedCorner.CornerRadius = UDim.new(0, 6)
speedCorner.Parent = SpeedInput
SpeedInput.Parent = MainFrame

-- === INFINITE AMMO ===
local ammoLabel = Instance.new("TextLabel")
ammoLabel.Size = UDim2.new(0.9, 0, 0, 22)
ammoLabel.Position = UDim2.new(0.05, 0, 0.66, 0)
ammoLabel.BackgroundTransparency = 1
ammoLabel.Text = "🔫 AMMO"
ammoLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
ammoLabel.Font = Enum.Font.GothamBold
ammoLabel.TextSize = 13
ammoLabel.TextXAlignment = Enum.TextXAlignment.Left
ammoLabel.Parent = MainFrame

createLine(MainFrame, 0.73)

local AmmoBtn = Instance.new("TextButton")
AmmoBtn.Size = UDim2.new(0.9, 0, 0, 28)
AmmoBtn.Position = UDim2.new(0.05, 0, 0.74, 0)
AmmoBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
AmmoBtn.Text = "∞ Infinite Ammo: ON"
AmmoBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
AmmoBtn.Font = Enum.Font.GothamBold
AmmoBtn.TextSize = 11
local ammoBtnCorner = Instance.new("UICorner")
ammoBtnCorner.CornerRadius = UDim.new(0, 6)
ammoBtnCorner.Parent = AmmoBtn
AmmoBtn.Parent = MainFrame

-- Stats
local StatsLabel = Instance.new("TextLabel")
StatsLabel.Size = UDim2.new(0.9, 0, 0, 16)
StatsLabel.Position = UDim2.new(0.05, 0, 0.86, 0)
StatsLabel.BackgroundTransparency = 1
StatsLabel.Text = "Kills: 0 | Fly: OFF"
StatsLabel.TextColor3 = Color3.fromRGB(255, 150, 200)
StatsLabel.Font = Enum.Font.Gotham
StatsLabel.TextSize = 9
StatsLabel.TextXAlignment = Enum.TextXAlignment.Left
StatsLabel.Parent = MainFrame

-- Controls
local ControlsLabel = Instance.new("TextLabel")
ControlsLabel.Size = UDim2.new(0.9, 0, 0, 30)
ControlsLabel.Position = UDim2.new(0.05, 0, 0.90, 0)
ControlsLabel.BackgroundTransparency = 1
ControlsLabel.Text = "WASD: Move | Space: Up | Shift: Down"
ControlsLabel.TextColor3 = Color3.fromRGB(255, 130, 180)
ControlsLabel.Font = Enum.Font.Gotham
ControlsLabel.TextSize = 9
ControlsLabel.TextWrapped = true
ControlsLabel.TextXAlignment = Enum.TextXAlignment.Left
ControlsLabel.Parent = MainFrame

-- ==========================================
-- BUTTON EVENTS
-- ==========================================

CloseBtn.MouseButton1Click:Connect(function()
    stopKillAura()
    stopFly()
    ScreenGui:Destroy()
end)

-- Aura Toggle
AuraBtn.MouseButton1Click:Connect(function()
    if not auraEnabled then
        startKillAura()
        AuraBtn.Text = "⏹ Aura OFF"
        AuraBtn.BackgroundColor3 = Color3.fromRGB(200, 40, 40)
        AuraStatus.Text = "Status: ON | Range: " .. currentRadius
        AuraStatus.TextColor3 = Color3.fromRGB(100, 255, 100)
    else
        stopKillAura()
        AuraBtn.Text = "▶ Aura ON"
        AuraBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 160)
        AuraStatus.Text = "Status: OFF | Range: " .. currentRadius
        AuraStatus.TextColor3 = Color3.fromRGB(255, 180, 210)
    end
end)

RadiusInput.FocusLost:Connect(function()
    local newRadius = tonumber(RadiusInput.Text)
    if newRadius then
        currentRadius = math.clamp(newRadius, CONFIG.MinRadius, CONFIG.MaxRadius)
        RadiusInput.Text = tostring(currentRadius)
        AuraStatus.Text = (auraEnabled and "Status: ON" or "Status: OFF") .. " | Range: " .. currentRadius
    else
        RadiusInput.Text = tostring(currentRadius)
    end
end)

-- Fly Toggle
FlyBtn.MouseButton1Click:Connect(function()
    if not flyEnabled then
        startFly()
        FlyBtn.Text = "⏹ Fly OFF"
        FlyBtn.BackgroundColor3 = Color3.fromRGB(200, 40, 40)
        FlyStatus.Text = "Status: ON | Speed: " .. CONFIG.FlySpeed
        FlyStatus.TextColor3 = Color3.fromRGB(100, 255, 100)
    else
        stopFly()
        FlyBtn.Text = "▶ Fly ON"
        FlyBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 160)
        FlyStatus.Text = "Status: OFF | Speed: " .. CONFIG.FlySpeed
        FlyStatus.TextColor3 = Color3.fromRGB(255, 180, 210)
    end
end)

SpeedInput.FocusLost:Connect(function()
    local newSpeed = tonumber(SpeedInput.Text)
    if newSpeed then
        CONFIG.FlySpeed = math.clamp(newSpeed, 5, 200)
        SpeedInput.Text = tostring(CONFIG.FlySpeed)
        FlyStatus.Text = (flyEnabled and "Status: ON" or "Status: OFF") .. " | Speed: " .. CONFIG.FlySpeed
    else
        SpeedInput.Text = tostring(CONFIG.FlySpeed)
    end
end)

-- Ammo Toggle
AmmoBtn.MouseButton1Click:Connect(function()
    CONFIG.InfiniteAmmo = not CONFIG.InfiniteAmmo
    if CONFIG.InfiniteAmmo then
        AmmoBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
        AmmoBtn.Text = "∞ Infinite Ammo: ON"
    else
        AmmoBtn.BackgroundColor3 = Color3.fromRGB(120, 60, 60)
        AmmoBtn.Text = "∞ Infinite Ammo: OFF"
    end
end)

-- Auto restart
player.CharacterAdded:Connect(function(newChar)
    task.wait(0.5)
    if auraEnabled then
        stopKillAura()
        task.wait(0.3)
        startKillAura()
    end
    if flyEnabled then
        stopFly()
        task.wait(0.3)
        startFly()
    end
end)

-- Update stats
spawn(function()
    while task.wait(1) do
        if ScreenGui and ScreenGui.Parent then
            StatsLabel.Text = "Kills: " .. totalKills .. " | Fly: " .. (flyEnabled and "ON" or "OFF")
        end
    end
end)

-- ==========================================
-- KHỞI TẠO
-- ==========================================

print("========================================")
print("  🌸 MIU HUB")
print("  Survive Zombie Arena")
print("  Loaded successfully!")
print("========================================")

-- Test remote
if gunHit then
    print("[MIU HUB] GunHit remote found:", gunHit:GetFullName())
else
    warn("[MIU HUB] GunHit NOT FOUND! Kill Aura may not work.")
    AuraStatus.Text = "⚠️ Remote not found!"
end
