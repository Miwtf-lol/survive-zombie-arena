--// MIU HUB - Survive Zombie Arena
--// Kill Aura + Fly GUI

pcall(function()
    game:GetService("CoreGui"):FindFirstChild("MIU_HUB"):Destroy()
end)

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()

local GunFire = ReplicatedStorage.GunRemotes.GunFire
local GunHit = ReplicatedStorage.GunRemotes.GunHit

local Aura = false
local Fly = false
local Range = 120
local FlySpeed = 60

local BV
local BG

--// CHARACTER UPDATE
LocalPlayer.CharacterAdded:Connect(function(char)
    Character = char
end)

--// GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MIU_HUB"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

--// NOTIFY
local Notify = Instance.new("Frame")
Notify.Parent = ScreenGui
Notify.Size = UDim2.new(0,0,0,50)
Notify.Position = UDim2.new(0.5,-100,0,-60)
Notify.BackgroundColor3 = Color3.fromRGB(255,105,180)
Notify.BorderSizePixel = 0

Instance.new("UICorner", Notify).CornerRadius = UDim.new(0,12)

local NotifyText = Instance.new("TextLabel")
NotifyText.Parent = Notify
NotifyText.Size = UDim2.new(1,0,1,0)
NotifyText.BackgroundTransparency = 1
NotifyText.Text = "MIU HUB FREE"
NotifyText.TextColor3 = Color3.new(1,1,1)
NotifyText.Font = Enum.Font.GothamBold
NotifyText.TextScaled = true

TweenService:Create(
    Notify,
    TweenInfo.new(0.5),
    {Size = UDim2.new(0,220,0,50), Position = UDim2.new(0.5,-110,0,20)}
):Play()

task.wait(2)

TweenService:Create(
    Notify,
    TweenInfo.new(0.5),
    {Position = UDim2.new(0.5,-110,0,-60)}
):Play()

task.wait(0.5)
Notify:Destroy()

--// MAIN
local Main = Instance.new("Frame")
Main.Parent = ScreenGui
Main.Size = UDim2.new(0,220,0,170)
Main.Position = UDim2.new(0.1,0,0.3,0)
Main.BackgroundColor3 = Color3.fromRGB(255,105,180)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true

Instance.new("UICorner", Main).CornerRadius = UDim.new(0,14)

local Title = Instance.new("TextLabel")
Title.Parent = Main
Title.Size = UDim2.new(1,0,0,30)
Title.BackgroundTransparency = 1
Title.Text = "MIU HUB"
Title.TextColor3 = Color3.new(1,1,1)
Title.Font = Enum.Font.GothamBold
Title.TextScaled = true

--// BUTTON CREATE
local function CreateButton(text, ypos)
    local Btn = Instance.new("TextButton")
    Btn.Parent = Main
    Btn.Size = UDim2.new(0.82,0,0,40)
    Btn.Position = UDim2.new(0.09,0,0,ypos)
    Btn.BackgroundColor3 = Color3.fromRGB(255,20,147)
    Btn.Text = text
    Btn.TextColor3 = Color3.new(1,1,1)
    Btn.Font = Enum.Font.GothamBold
    Btn.TextScaled = true
    Btn.BorderSizePixel = 0

    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0,12)

    return Btn
end

local AuraBtn = CreateButton("KILL AURA : OFF",40)
local FlyBtn = CreateButton("FLY : OFF",90)

--// TOGGLE
AuraBtn.MouseButton1Click:Connect(function()
    Aura = not Aura

    if Aura then
        AuraBtn.Text = "KILL AURA : ON"
    else
        AuraBtn.Text = "KILL AURA : OFF"
    end
end)

--// FLY
local function StartFly()
    local HRP = Character:WaitForChild("HumanoidRootPart")

    BV = Instance.new("BodyVelocity")
    BV.MaxForce = Vector3.new(999999,999999,999999)
    BV.Velocity = Vector3.zero
    BV.Parent = HRP

    BG = Instance.new("BodyGyro")
    BG.MaxTorque = Vector3.new(999999,999999,999999)
    BG.P = 1000
    BG.Parent = HRP

    RunService.RenderStepped:Connect(function()
        if Fly and BV and BG then
            local cam = workspace.CurrentCamera
            BG.CFrame = cam.CFrame

            local move = Vector3.zero

            if UIS:IsKeyDown(Enum.KeyCode.W) then
                move += cam.CFrame.LookVector
            end

            if UIS:IsKeyDown(Enum.KeyCode.S) then
                move -= cam.CFrame.LookVector
            end

            if UIS:IsKeyDown(Enum.KeyCode.A) then
                move -= cam.CFrame.RightVector
            end

            if UIS:IsKeyDown(Enum.KeyCode.D) then
                move += cam.CFrame.RightVector
            end

            BV.Velocity = move * FlySpeed
        end
    end)
end

local function StopFly()
    if BV then BV:Destroy() end
    if BG then BG:Destroy() end
end

FlyBtn.MouseButton1Click:Connect(function()
    Fly = not Fly

    if Fly then
        FlyBtn.Text = "FLY : ON"
        StartFly()
    else
        FlyBtn.Text = "FLY : OFF"
        StopFly()
    end
end)

--// GUN
local function GetGun()
    for _,v in pairs(Character:GetChildren()) do
        if v:IsA("Tool") then
            return v
        end
    end
end

--// KILL AURA
RunService.RenderStepped:Connect(function()
    if not Aura then return end
    if not Character then return end

    local HRP = Character:FindFirstChild("HumanoidRootPart")
    if not HRP then return end

    local Gun = GetGun()
    if not Gun then return end

    for _,Zombie in pairs(workspace:GetDescendants()) do
        if Zombie:IsA("Model")
        and Zombie ~= Character
        and string.find(Zombie.Name:lower(),"zombie") then

            local Humanoid = Zombie:FindFirstChildOfClass("Humanoid")
            local Root = Zombie:FindFirstChild("HumanoidRootPart")

            if Humanoid and Root and Humanoid.Health > 0 then
                local Distance = (HRP.Position - Root.Position).Magnitude

                if Distance <= Range then
                    pcall(function()

                        GunFire:FireServer(
                            Gun,
                            Root.Position,
                            Root
                        )

                        GunHit:FireServer(
                            Zombie,
                            Humanoid,
                            Root,
                            Root.Position
                        )

                    end)
                end
            end
        end
    end
end)
